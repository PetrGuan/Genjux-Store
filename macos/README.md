# Genjux-Store macOS App

Native AppKit app (Phase 1, see [`../ROADMAP.md`](../ROADMAP.md) and [`../.copilot-workflow/PLAN.md`](../.copilot-workflow/PLAN.md) section 6.1), programmatic UI only — no Storyboards/XIBs, no SwiftUI. A thin client of the core service's local HTTP API (see [`../core`](../core)); it does not talk to GitHub directly itself.

## Building

The Xcode project itself is generated by [XcodeGen](https://github.com/yonaskolb/XcodeGen) from [`project.yml`](project.yml) and is **not** committed — regenerate it after pulling or after editing `project.yml` / adding or removing source files:

```bash
brew install xcodegen  # if you don't already have it
cd macos
xcodegen generate
open GenjuxStore.xcodeproj
```

Or build from the command line:

```bash
cd macos
xcodegen generate
xcodebuild -project GenjuxStore.xcodeproj -scheme GenjuxStore -configuration Debug build
```

The app itself lazily starts and talks to a `genjuxd` core service via the local HTTP API (see [`../core/src/lifecycle.rs`](../core/src/lifecycle.rs) for the on-disk singleton-lock/discovery-file protocol, and [`GenjuxStore/Core/ServiceLifecycle.swift`](GenjuxStore/Core/ServiceLifecycle.swift) for the Swift side of it — the same protocol the CLI uses, so a GUI and CLI session correctly share one running instance).

## Testing

`GenjuxStoreTests` includes a real end-to-end test against the actual `genjuxd` binary (skipped, not failed, if it hasn't been built yet):

```bash
cargo build --bin genjuxd  # from the repo root, first
cd macos
xcodegen generate
xcodebuild -project GenjuxStore.xcodeproj -scheme GenjuxStore -configuration Debug test
```

## Status

Phase 1 is in progress — see the [Phase 1 epic (#23)](https://github.com/PetrGuan/Genjux-Store/issues/23) for the current sub-issue checklist. So far: the initial project scaffold (#58), the local core-service client + lazy-start (#59), the Home screen (#60, a real recommended-macOS-apps card grid backed by `GET /discover/macos`), the Search screen (#61, an `owner/repo` toolbar search field wired to `GET /repos/:owner/:repo/packages`), the App detail screen (#62, README excerpt/star count/last-release date via `GET /repos/:owner/:repo/metadata`), and the Install progress screen (#63, a real `POST /install` + `GET /installs/:id` polling loop — Install buttons on the Home card and App detail screen are now fully wired, not placeholders). The installed/updates screen lands in #64.
